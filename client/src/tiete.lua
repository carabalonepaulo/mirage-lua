-- Aqui é onde ocorre toda a poluição do escopo global.

class = require 'lib.30log'

--[[PacketWriter = require 'src.packet.writer'
PacketReader = require 'src.packet.reader']]

Emitter = require 'src.emitter'
Input = require 'src.input'
Yui = require 'src.yui'
Scene = require 'src.scene'

Control = require 'src.control'
Button = require 'src.control.button'
TextInput = require 'src.control.text_input'

Network = require('src.network')()
